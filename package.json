{
  "name": "rem",
  "version": "0.1.0",
  "private": true,
  "description": "rem - read everything minder",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "convex:logs": "convex logs"
  },
  "devDependencies": {
    "convex": "^1.17.0",
    "lefthook": "^2.1.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
